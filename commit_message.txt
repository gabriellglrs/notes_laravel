feat: Implementa a página inicial do usuário

Este commit introduz a página inicial que é exibida ao usuário após o login.

- O `MainController` foi atualizado para buscar os dados do usuário da sessão e renderizar a nova view `home.blade.php`.
- A nova view `home.blade.php` foi criada para exibir as informações do usuário.
- O `AuthController` agora redireciona explicitamente para a página de login em caso de falha na autenticação.
- O nome do projeto foi corrigido no `package-lock.json`.